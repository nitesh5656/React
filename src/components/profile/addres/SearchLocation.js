import { View, Text, TouchableOpacity } from "react-native";
import React from "react";
import { useState } from "react";

export default function SearchLocationPanel({ navigation }) {
    const [location, setLocation] = useState();
  return (
    <View>

  </View>
  )
}