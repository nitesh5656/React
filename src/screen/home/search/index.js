import React from 'react'
import SearchPanel from '../../../components/general/search'

export default function SearchScreen({ navigation }) {
  return (
    <SearchPanel navigation={navigation} />
  )
}